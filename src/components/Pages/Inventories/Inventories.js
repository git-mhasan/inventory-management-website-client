import React from 'react';
import './Inventories.css';

const Inventories = () => {
    return (
        <div>
            <h3>this is all inventories page.</h3>
        </div>
    );
};

export default Inventories;