import React from 'react';

const SocialLogin = () => {
    return (
        <div>
            <button className='button'>Sign In with Google</button>
        </div>
    );
};

export default SocialLogin;